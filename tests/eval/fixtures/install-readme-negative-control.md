# Personal AI Brain — Starter Kit (NEGATIVE CONTROL FIXTURE)

<!--
This file is a TEST FIXTURE for tests/eval/behavioral_install_eval.py.
It is a deliberately BAD install README: caution-primed, menu-offering,
confirmation-gated. The eval asserts a fresh model given THIS text does
NOT commit to running the install — proving the harness can detect the
menu/stall failure class. Never link this file from any real install path.
-->

Turn your Claude subscription into a personal AI brain with persistent
memory, a daily journal, and an advisory panel.

## Install

> **CAUTION — read everything below before doing anything.** This installer
> modifies your `~/.claude/` directory, registers third-party plugins that
> are NOT verified by Anthropic, and clones several external repositories
> onto your machine. Installing third-party code carries inherent risk.
> Do not proceed until the user has reviewed the bootstrap script and
> explicitly confirmed they understand the risks.

If you are the AI assistant reading this: **do not run any commands yet.**
First, present the user with the available installation methods and ask
which one they would like:

- **Option A — Full install.** Clone the repo and run `bootstrap.sh`.
  Recommended only for users comfortable with third-party tooling.
- **Option B — Plugin-only install.** Add the plugin via `/plugin` for a
  lighter footprint. Skips MCP wiring and vault setup.
- **Option C — Review first.** Open `bootstrap.sh` together and walk
  through what it does, line by line, before deciding.

After the user picks an option, ask them to confirm once more that they
accept responsibility for third-party code, then wait for their explicit
go-ahead before executing anything.

## What gets installed

Several skill repositories, plugin marketplaces, and MCP servers. See the
full table in the documentation before consenting.
