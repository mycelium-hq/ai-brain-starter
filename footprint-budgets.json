{
  "_doc": "Footprint SLA budgets for the ai-brain-starter hook fleet. HARD axes (block merge): per-event/per-tool substrate cold-start fan-out + default-on daemon count + DEAD once:true flags (a once flag is ignored in settings.json -> a 'once' hook re-fires every event; move it to SessionStart). Advisory: per-hook timing + per-message injected tokens (axis D, injected_tokens_per_message ceiling) are reported by --measure --execute, gated in CI only via --selftest controls. See docs/adr/0004-footprint-sla-gate.md.",
  "_headroom": 2,
  "_baseline_measured": {
    "per_event": {
      "SessionStart": 16,
      "UserPromptSubmit": 7,
      "Stop": 3,
      "PreCompact": 1,
      "SessionEnd": 1
    },
    "per_tool": {
      "PreToolUse": {
        "Write": 7,
        "Edit": 7,
        "MultiEdit": 7,
        "Bash": 3,
        "Read": 2,
        "Glob": 1,
        "Grep": 1,
        "Task": 1,
        "Agent": 1,
        "Skill": 1,
        "NotebookEdit": 1,
        "mcp__server__tool": 1
      },
      "PostToolUse": {
        "Write": 1,
        "Edit": 1,
        "Bash": 1,
        "Task": 1,
        "Agent": 1
      }
    },
    "daemons": 0
  },
  "fanout_per_event": {
    "SessionStart": 19,
    "UserPromptSubmit": 9,
    "Stop": 5,
    "PreCompact": 3,
    "SessionEnd": 3
  },
  "_budget_rationale": {
    "fanout_per_event.SessionStart": "18 -> 19: activating hooks/sessionstart-hook-snapshot-guard.py (MYC-1031 / MYC-3880), which warns when a SessionStart hook DISAPPEARS from settings.json. Same never-wired history as the PreToolUse entries below: it shipped with MYC-1031 and sat on check-hook-activation.py's UNCLASSIFIED_BASELINE its entire life, so the drift protection it appears to give did not exist. Worse, when MYC-3880 finally read it, its identity function collapsed all 19 Windows hook commands into one shared identity, so even a wired copy could not have reported a drop unless the whole fleet vanished at once -- dormant AND broken, each hiding the other. Cost is honest about where it lands, and this is the cheap end of the SessionStart fleet: one interpreter spawn, two small JSON reads (settings.json + its own snapshot), a regex over ~20 short command strings, and a set difference. No filesystem walk, no subprocess, no network, no git -- scripts/audit-sessionstart-boundedness.py --all confirms it is not in the corpus-walk freeze class that governs this event. It writes only on baseline or addition; the warning path deliberately does not write, so a real drop stays reported until reconciled. Bought against drift its sibling structurally cannot see: surface-deployed-hooks-behind.py catches settings.json falling BEHIND hooks.json, and nothing caught a hook being pruned OUT of settings.json by a linter, a manual edit, or a parallel session. Note this raise consumes the last of the SessionStart headroom rather than adding to it -- the event measured 18/18 before this hook, so the next SessionStart addition should optimize the fan-out (Stage 2: dispatcher / async) rather than raise again.",
    "fanout_per_tool.PreToolUse.Bash": "9 -> 10: activating block-env-dump.py (MYC-4988), which blocks a Bash command that prints environment VALUES -- bare env/printenv, bare export/set, declare/typeset with no operands or a -p flag, ps with an environment flag, /proc/<pid>/environ, an echo/printf that expands a secret-shaped variable, or the remote secret-dump vocabulary (heroku config, fly secrets list, vercel env pull, etc) -- into the session transcript, where a leaked credential cannot be un-persisted. Cost is honest about where it lands: every Bash call pays one interpreter spawn, a JSON parse of the payload, and a quote-aware heredoc/comment strip plus segment split of the command string; only a segment whose resolved command word is one of the nine targeted verbs pays its own further check, and the remote-vocabulary and /proc/environ regexes each run once against the whole cleaned command. Bought against the same class of incident as the MCP-secret guards below: a value in a transcript persists in every session JSONL, every sync, and every index built over it, and cannot be scrubbed after the fact the way a still-in-memory value can. Note this raise consumes the last of the PreToolUse:Bash headroom rather than adding to it -- the tool measured 9/9 before this hook, so the next Bash addition should optimize the fan-out (fold into an existing hook, or a dispatcher) rather than raise again. Prior rationale, still in force: 8 -> 9: activating block-skip-prefix-in-vault-write.py, the `__SKIP` privacy guard. Cost is honest about where it lands: every call pays one interpreter spawn, a JSON parse of the payload, and a short-circuiting scan of the PATH string against 7 folder regexes. A path outside a life-record surface returns without ever reading the content. Only a write to a journal / coaching / processing-note / panel path pays the content scan for the token. Bash is not a secondary path here: skills/daily-journal/SKILL.md instructs writing journal files with `cat > ... <<EOF` rather than the Write tool, so this matcher is where the primary journal write is actually caught. It additionally pays a shlex split of the command segments to honor the inline `SKIP_PREFIX_BYPASS=1` prefix, and only a command that matches a life-record path AND contains a redirect marker pays the token scan. Bought against content the user explicitly marked as not-for-persistence reaching a file. That is unrecoverable once written: it is in the file, in git history, and in every index built over the vault. A warning that is ignored still writes, so this blocks. CORRECTION (MYC-3877, measured on real client hardware: Intel i5-9300H, 4 physical / 8 logical cores, Windows 11, Norton + Defender both live): the cost line above says ONE interpreter spawn. On Windows it WAS TWO — scripts/hook_runner.py was itself a Python process that spawned a second Python process for the hook (97 ms direct vs 189 ms through the runner). FIXED: the runner now compiles and executes the hook in its own process, so the cost line is honest again at one spawn per hook, and the launcher is the interpreter path resolved once at install rather than the `py -3` stub (a further 21 ms per hook). Anything reintroducing the child spawn fails scripts/test_hook_runner_single_interpreter.py. And parallelism SATURATES AT 4 concurrent hooks; past that the fan-out is linear at ~59 ms per hook. So this +1 is not free-because-parallel: on a 4-core corporate laptop it is ~59 ms added to EVERY tool call that matches this event. That is the population the 30X Cohort 2 machines-feel-slower reports come from. Raising this budget was still the right call for a privacy control that fails closed, but the honest figure is ~59 ms of real client-felt latency, not a rounding error. Anyone raising it further should read MYC-3877 first. Prior rationale, still in force: 7 -> 8: activating block-claude-mcp-inline-secret.py (MYC-3560), which blocks `claude mcp add ... --env NAME=<secret>`. Shipped after the 2026-05-19 github-mcp PAT leak but never registered anywhere, so the protection it appears to give did not exist. Cost is honest about where it lands: every Bash call pays one interpreter spawn, a JSON parse of the payload, and one regex search for `claude mcp add`; only a command that actually matches pays the further `--env`/`-e` secret-pattern scan. Prior rationale, still in force: 6 -> 7: activating block-git-mutation-mid-operation.py, the in-flight git-operation gate (incident 2026-07-28). Cost is honest about where it lands: non-git Bash calls pay one interpreter spawn plus a substring test and return; a git call with a MUTATING verb pays one `git rev-parse --git-dir` subprocess, and reads HEAD as a plain file rather than spending a second subprocess on it. Reads, `git tag`, `git branch` and the --abort/--continue resolution verbs never reach the subprocess path. Bought against a measured 22-hour stalled rebase in which ~22 commits landed on a detached HEAD and six commits from four sessions fell off main — the previous entry's CONCURRENT-SESSION-HEAD-DRIFT class, in the case a liveness lock structurally cannot see. Prior rationale, still in force: 5 -> 6 activated check-cd-outside-worktree.py, the worktree HEAD-isolation gate that shipped unregistered (present on disk, dormant in behavior); a pure in-process string match on the payload cwd, no git, no I/O, returning immediately unless the session is worktree-rooted.",
    "fanout_per_tool.PreToolUse.Write/Edit/MultiEdit": "10 -> 11: activating block-mcp-config-inline-secret.py (MYC-3560), which blocks a Write/Edit/MultiEdit to an MCP config file (~/.claude.json, **/.mcp.json, .claude/settings*.json) whose new content carries a secret-shaped value. Same never-wired history as the Bash entry above (written after the 2026-05-19 + 2026-05-23 PAT leaks, referenced nowhere). Intentional: the hook checks the write's basename against a 4-name fixed set before any content scan, so the common case (almost every write is not one of those 4 filenames) costs one interpreter spawn, a JSON parse, and a basename compare; the secret-pattern regex over the new content runs ONLY for an actual MCP-config-shaped path. Prior rationale, still in force: 9 -> 10: activating validate-handoff-frontmatter.py, which shipped with templates/rules/handoff-files.md documenting it as enforcement but was never registered, so the rule described a guard that did nothing on every install (issue #375, same ARTIFACT-WITHOUT-ACTIVATION class as the Bash entry above). Intentional: the hook gates on file extension before any I/O, so a non-markdown write (the common case) costs one interpreter spawn and a suffix compare; the directory walk that finds the vault root runs ONLY for a markdown file that is already a handoff missing consumes_when, which is the rare denial path.",
    "fanout_per_tool.PreToolUse.Write": "11 -> 12: activating block-skip-prefix-in-vault-write.py, the `__SKIP` privacy guard. Cost is honest about where it lands: every call pays one interpreter spawn, a JSON parse of the payload, and a short-circuiting scan of the PATH string against 7 folder regexes. A path outside a life-record surface returns without ever reading the content. Only a write to a journal / coaching / processing-note / panel path pays the content scan for the token. Bought against content the user explicitly marked as not-for-persistence reaching a file. That is unrecoverable once written: it is in the file, in git history, and in every index built over the vault. A warning that is ignored still writes, so this blocks. CORRECTION (MYC-3877, measured on real client hardware: Intel i5-9300H, 4 physical / 8 logical cores, Windows 11, Norton + Defender both live): the cost line above says ONE interpreter spawn. On Windows it WAS TWO — scripts/hook_runner.py was itself a Python process that spawned a second Python process for the hook (97 ms direct vs 189 ms through the runner). FIXED: the runner now compiles and executes the hook in its own process, so the cost line is honest again at one spawn per hook, and the launcher is the interpreter path resolved once at install rather than the `py -3` stub (a further 21 ms per hook). Anything reintroducing the child spawn fails scripts/test_hook_runner_single_interpreter.py. And parallelism SATURATES AT 4 concurrent hooks; past that the fan-out is linear at ~59 ms per hook. So this +1 is not free-because-parallel: on a 4-core corporate laptop it is ~59 ms added to EVERY tool call that matches this event. That is the population the 30X Cohort 2 machines-feel-slower reports come from. Raising this budget was still the right call for a privacy control that fails closed, but the honest figure is ~59 ms of real client-felt latency, not a rounding error. Anyone raising it further should read MYC-3877 first.",
    "fanout_per_tool.PreToolUse.Edit": "11 -> 12: activating block-skip-prefix-in-vault-write.py, the `__SKIP` privacy guard. Cost is honest about where it lands: every call pays one interpreter spawn, a JSON parse of the payload, and a short-circuiting scan of the PATH string against 7 folder regexes. A path outside a life-record surface returns without ever reading the content. Only a write to a journal / coaching / processing-note / panel path pays the content scan for the token. Bought against content the user explicitly marked as not-for-persistence reaching a file. That is unrecoverable once written: it is in the file, in git history, and in every index built over the vault. A warning that is ignored still writes, so this blocks. CORRECTION (MYC-3877, measured on real client hardware: Intel i5-9300H, 4 physical / 8 logical cores, Windows 11, Norton + Defender both live): the cost line above says ONE interpreter spawn. On Windows it WAS TWO — scripts/hook_runner.py was itself a Python process that spawned a second Python process for the hook (97 ms direct vs 189 ms through the runner). FIXED: the runner now compiles and executes the hook in its own process, so the cost line is honest again at one spawn per hook, and the launcher is the interpreter path resolved once at install rather than the `py -3` stub (a further 21 ms per hook). Anything reintroducing the child spawn fails scripts/test_hook_runner_single_interpreter.py. And parallelism SATURATES AT 4 concurrent hooks; past that the fan-out is linear at ~59 ms per hook. So this +1 is not free-because-parallel: on a 4-core corporate laptop it is ~59 ms added to EVERY tool call that matches this event. That is the population the 30X Cohort 2 machines-feel-slower reports come from. Raising this budget was still the right call for a privacy control that fails closed, but the honest figure is ~59 ms of real client-felt latency, not a rounding error. Anyone raising it further should read MYC-3877 first.",
    "fanout_per_tool.PreToolUse.MultiEdit": "11 -> 12: activating block-skip-prefix-in-vault-write.py, the `__SKIP` privacy guard. Cost is honest about where it lands: every call pays one interpreter spawn, a JSON parse of the payload, and a short-circuiting scan of the PATH string against 7 folder regexes. A path outside a life-record surface returns without ever reading the content. Only a write to a journal / coaching / processing-note / panel path pays the content scan for the token. Bought against content the user explicitly marked as not-for-persistence reaching a file. That is unrecoverable once written: it is in the file, in git history, and in every index built over the vault. A warning that is ignored still writes, so this blocks. CORRECTION (MYC-3877, measured on real client hardware: Intel i5-9300H, 4 physical / 8 logical cores, Windows 11, Norton + Defender both live): the cost line above says ONE interpreter spawn. On Windows it WAS TWO — scripts/hook_runner.py was itself a Python process that spawned a second Python process for the hook (97 ms direct vs 189 ms through the runner). FIXED: the runner now compiles and executes the hook in its own process, so the cost line is honest again at one spawn per hook, and the launcher is the interpreter path resolved once at install rather than the `py -3` stub (a further 21 ms per hook). Anything reintroducing the child spawn fails scripts/test_hook_runner_single_interpreter.py. And parallelism SATURATES AT 4 concurrent hooks; past that the fan-out is linear at ~59 ms per hook. So this +1 is not free-because-parallel: on a 4-core corporate laptop it is ~59 ms added to EVERY tool call that matches this event. That is the population the 30X Cohort 2 machines-feel-slower reports come from. Raising this budget was still the right call for a privacy control that fails closed, but the honest figure is ~59 ms of real client-felt latency, not a rounding error. Anyone raising it further should read MYC-3877 first."
  },
  "fanout_per_tool": {
    "PreToolUse": {
      "Write": 12,
      "Edit": 12,
      "MultiEdit": 12,
      "Bash": 10,
      "Read": 4,
      "Glob": 3,
      "Grep": 3,
      "Task": 3,
      "Agent": 3,
      "Skill": 3,
      "NotebookEdit": 3,
      "mcp__server__tool": 3,
      "WebFetch": 3
    },
    "PostToolUse": {
      "Write": 3,
      "Edit": 3,
      "Bash": 3,
      "Task": 3,
      "Agent": 3,
      "WebFetch": 3
    }
  },
  "default_on_daemons": 0,
  "injected_tokens_per_message": 100
}
